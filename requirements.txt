# Project Aura - Python Backend Dependencies
# Target runtime: Python 3.11+, Modal deployment.

# Core orchestration
pipecat-ai[livekit]==0.0.108
modal

# Web framework (session bootstrap API)
fastapi
uvicorn[standard]

# Session state
redis[asyncio]

# Data validation / settings
pydantic>=2.0
pydantic-settings

# Utilities
python-dotenv
structlog
httpx>=0.25.0

# Testing
pytest
pytest-asyncio
httpx

# ---------------------------------------------------------------------------
# Voice stack providers
# ---------------------------------------------------------------------------

# LLM — standard path
anthropic

# LLM — demo lane (vLLM OpenAI-compatible endpoint on Modal)
openai>=1.0.0

# STT — Deepgram
deepgram-sdk>=3.2.0

# Transport — LiveKit
livekit-api

# TTS — Fish Audio S2 Pro (self-hosted via modal_fish.py)
# Fish Audio uses httpx (already listed above); no extra SDK required.

# Memory store (persistent) — deferred
# supabase
# asyncpg
# aiosqlite